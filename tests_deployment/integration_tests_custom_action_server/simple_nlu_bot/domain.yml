version: "3.1"

intents:
  - greet
  - goodbye

responses:
  utter_greet:
  - text: "Hi"

actions:
  - action_goodbye

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
