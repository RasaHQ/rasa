action_endpoint:
  url: "http://action-server:5055/webhook"

nlg:
  type: rephrase

tracing:
  type: otlp
  endpoint: otlp-collector:4317
  insecure: false
  service_name: rasa
  root_certificates: ./certs/ca.pem

metrics:
  type: otlp
  endpoint: otlp-collector:4317
  insecure: false
  service_name: rasa
  root_certificates: ./certs/ca.pem
