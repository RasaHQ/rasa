recipe: default.v1
language: en
pipeline:
- name: SingleStepLLMCommandGenerator

policies:
- name: FlowPolicy
- name: EnterpriseSearchPolicy
  max_messages_in_query: 1
  # CI/OpenAI: defaults are timeout 10s and max_retries 1, which flakes under load.
  llm:
    timeout: 90
    max_retries: 3
  embeddings:
    timeout: 60
    max_retries: 3
- name: IntentlessPolicy

assistant_id: 20240219-110701-dichotomic-inference
