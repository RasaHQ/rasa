{
  "name": "rasa",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "yarn run pre-build && docusaurus start",
    "pre-build": "yarn run variables && yarn run program-outputs && yarn run included-sources",
    "build": "docusaurus build",
    "new-version": "docusaurus docs:version",
    "variables": "node compile_variables.js",
    "program-outputs": "node compile_program_outputs.js",
    "included-sources": "node compile_included_sources.js"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.56",
    "@docusaurus/preset-classic": "^2.0.0-alpha.56",
    "classnames": "^2.2.6",
    "fs-extra": "^9.0.1",
    "globby": "^10.0.1",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "remark": "^12.0.0",
    "remark-collapse": "^0.1.2",
    "remark-sources": "RasaHQ/remark-sources#19255f0",
    "unist-util-visit-children": "^1.1.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "toml": "^3.0.0"
  }
}
