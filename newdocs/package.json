{
  "name": "rasa",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "yarn run variables && docusaurus start",
    // build happens on the `documentation` branch, where
    // we expect the variables to be already computed
    "build": "docusaurus build",
    "new-version": "docusaurus docs:version",
    "variables": "node compile_variables.js"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.56",
    "@docusaurus/preset-classic": "^2.0.0-alpha.56",
    "classnames": "^2.2.6",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "remark": "^12.0.0",
    "remark-collapse": "^0.1.2",
    "remark-sources": "^1.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "toml": "^3.0.0"
  }
}
