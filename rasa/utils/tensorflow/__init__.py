# This build targets Python 3.12/3.13, where TensorFlow has no wheels. The
# TensorFlow-backed components and low-level TF layers have been removed from this
# distribution; only the TensorFlow-free helpers in this package remain
# (constants, model_data, feature_array, model_data_utils).
