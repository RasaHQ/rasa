event_broker:
  type: pika
  url: localhost
  username: username
  password: password
  queue: queue  # to publish to a single queue
#  alternatively, you can publish to multiple queues with:
#  queues:
#    - queue-1
#    - queue-2
