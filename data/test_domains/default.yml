intents:
 - greet
 - default
 - goodbye

slots:
  cuisine:
    type: text
    mappings:
      - type: from_text
  location:
    type: text
    mappings:
      - type: from_text

entities:
 - name

responses:
  utter_greet:
    - text: hey there!
  utter_goodbye:
    - text: goodbye :(
  utter_default:
    - text: default message
