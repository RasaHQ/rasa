version: "2.0"

intents:
  - request_restaurant:
      use_entities:
        - cuisine
        - feedback
  - inform

entities:
  - cuisine
  - feedback

session_config:
  session_expiration_time: 60  # value in minutes
  carry_over_slots_to_new_session: true
