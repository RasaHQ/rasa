version: "3.1"

intents:
  - request_restaurant:
      use_entities:
        - cuisine
        - seating
  - inform

entities:
  - cuisine
  - feedback
  - seating

session_config:
  session_expiration_time: 60  # value in minutes
  carry_over_slots_to_new_session: true
