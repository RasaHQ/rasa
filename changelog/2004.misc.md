Added validation for `config.yml` language settings, raising exceptions when the default `language` appears in `additional_languages` or invalid language codes are provided, and for response and flow translations by issuing warnings when translations are missing.