The `UsageStatistics` was updated, and the `usage_statistics.model_dump()` was trying to
set the params of the `log_copilot_from_handler` that weren't there. This bugfix
addresses this issue.

Set minimum Rasa Pro version to 3.14 for downloaded bot projects.
