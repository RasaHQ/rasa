Fixed missing `await` on `sio.enter_room()` in the SocketIO channel's `session_request` handler. Without this fix, session persistence silently fails because the coroutine is never actually awaited, so the sender socket never joins the session room.
