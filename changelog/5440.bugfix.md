Fixed `utter_corrected_previous_input` template to explicitly pair each slot name with its corrected value, preventing LLM rephrasers from swapping the associations when multiple slots are corrected at once.
