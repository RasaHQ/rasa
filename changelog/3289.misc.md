Fixed intermittent CI failures caused by test state leakage by implementing proper cleanup of temporary directories and `sys.path` entries in test fixtures, and centralized `OPENAI_API_KEY` environment variable setup.