Implemented `rasa tools init`, an interactive setup wizard that configures a local MCP
server for AI coding assistants. The wizard guided users through selecting a transport
mode (stdio or HTTP), documentation mode (offline or online), IDE integrations (Cursor,
VS Code, Claude Code, JetBrains), and optional agent skills installation.
