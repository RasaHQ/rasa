Dialogue Understanding Tests (DUT) now skip rephrase endpoint validation.

Previously, running DUT would fail with a validation error when the domain contained responses with `metadata.rephrase: true` as the rephraser is intentionally disabled in DUT.
