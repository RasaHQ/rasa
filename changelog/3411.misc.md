Fixed `test_template_loads_actions_module` test failure caused by cross-test contamination:
- Added global cache clearing fixture for `DirectCustomActionExecutor` to prevent cached actions from affecting subsequent tests.
- Updated async mocks in `tests/builder/test_jobs.py` for better async/await pattern handling
- Cleaned up warnings from test interference in `tests/test_default_project.py` to improve test output clarity
