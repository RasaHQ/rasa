Added new functionality in `TransformerRasaModel` and `rasa_layers.py` classes which enables us updating `DenseForSparse` layers' kernels if sparse feature sizes are changed during incremental training.