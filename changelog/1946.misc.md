Added the `additional_languages` field to extend multi-language support. Updated the `GraphModelConfiguration`, `GraphV1Recipe`, and `DefaultV1Recipe` to pass the new setting from `config.yml`, and modified `LocalModelStorage` to include it in model metadata.