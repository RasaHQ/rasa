Fixed crash when running `rasa test` on projects containing flows with `call:` steps that reference agents.

The `rasa test` command now correctly initializes the agent registry by calling `Configuration.initialise_sub_agents()`, aligning its behavior with other CLI commands (`rasa train`, `rasa shell`, `rasa e2e test`, `rasa inspect`, `rasa data`).
