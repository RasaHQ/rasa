Updated the `Flow.readable_name` method to return a language-specific flow name when a translation is available. Revised the pattern commands (`ClarifyCommand`, `CancelFlowCommand`, and `ContinueInterruptedPatternFlowStackFrame`) to retrieve the current language from the tracker and display the corresponding localized flow name.