Added `rasa tools init skills` subcommand to fetch and install agent skills with
version compatibility checks and overwrite confirmation prompts.

Added `rasa tools init docs` subcommand to fetch offline documentation files, with
automatic detection of online documentation mode to skip the download when the project
is configured for online use.
