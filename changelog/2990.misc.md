Improved assistant tracker context handling in copilot with better conversation history
formatting and configurable conversation history limits. Conversation history is now
limited to a configurable number of turns, where one turn represents a complete
interaction between the user and assistant.