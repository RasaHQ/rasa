Add run_eagerly parameter to control whether to compile DIET-based model before running or not

Saves computation time when the user preliminarily knows that model will be executed only for a few steps.