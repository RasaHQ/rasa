Add config persistence and auto-save for `rasa tools run`. Fix file operation tools not 
accessing the server-level project folder global.

Fix file operation tools (`list_project_files`, `read_file`, `update_files`) failing
because they read the project folder from an unset environment variable instead of the
MCP server global.