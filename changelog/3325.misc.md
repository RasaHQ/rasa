Implemented server-side copilot chat history management using SQLite storage backends.

The `/api/copilot` endpoint now automatically stores Copilot's conversation history server-side, 
while new `/api/copilot/history` endpoints enable retrieving and deleting conversation history. 
