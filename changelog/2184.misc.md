Added the possibility to highlight debug log messages by adding `highlight=True`.
The log messages will be surrounded by `=`.

Added the possibility to format json values properly: List all fields that should be
formatted under `json_formatting`, e.g. `json_formatting=["<field_name>"]`.
