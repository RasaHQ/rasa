Introduced an optional `summarize_converstaion` flag in response metadata,
defaulting to `False`. When set to `False`, the conversation transcript
(up to the last five turns) instead of the summary is included in the prompt.
This saves a separate summarization call to an LLM.
