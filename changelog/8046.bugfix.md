`ResponseSelector` can now be trained when `use_text_as_label=true` and a transformer is
enabled (i.e. a positive number of transformer layers is provided), even if one doesn't
specify the transformer's size.