Events streamed to the event broker now include `user_id` in the message payload alongside `sender_id`.
This allows downstream consumers to identify the end user associated with each conversation event.
