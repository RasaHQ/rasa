Add new `Training Failed` telemetry event, together with the `assistant_id` property on all training related events
and `Shell Started`.
Remove duplicate telemetry events for `Training Started` and `Inspect Started`.
