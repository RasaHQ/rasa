Added `StrictCategoricalSlot` to enforce that only predetermined string values (or `None`) can be set, raising an error when an invalid value is provided.

 Added a built-in language slot (of `StrictCategoricalSlot` type) that uses the `language` setting from `config.yml` as its initial value, with allowed values comprising the `language` and `additional_languages`.