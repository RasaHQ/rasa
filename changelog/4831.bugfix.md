Fixes MCP tool result handling to now process results that contain only ``structuredContent`` (and no or empty ``content``).
Previously, an empty ``content`` field caused the result to be skipped and slots were not set from ``structuredContent``.
