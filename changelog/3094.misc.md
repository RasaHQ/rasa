Added opt‑in signing/verification for Copilot chat history.
Implementation emits SSE signature envelopes for assistant messages to enable FE chaining. 
Enforced verification when clients opt in via `signature_version`.

Configurable via `COPILOT_HISTORY_SIGNING_SECRET`
