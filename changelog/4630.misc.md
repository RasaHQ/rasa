Implemented `rasa tools run` command to start the MCP server standalone with support for both stdio (default, for IDE integrations) and HTTP (for testing) transport modes. Added project path resolution with CLI arg → env var → cwd cascade, health endpoint for HTTP mode, and proper logging redirection to stderr for stdio mode to prevent protocol corruption.
